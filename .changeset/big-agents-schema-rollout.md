---
"@stepkit/core": major
"@stepkit/cli": major
---

Replace the legacy agent configuration shape with the unified `customProviders` and `agents.*.items` schema. CLI docs now cover `stepkit init` and `stepkit agents` for configuring providers, reusable agent entries, and workflow role mappings.
