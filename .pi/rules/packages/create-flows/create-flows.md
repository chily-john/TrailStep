# TrailStep create-flows rule

The package build inlines markdown prompt fragments with the `tsup` text loader, so no non-bundled output and no fragment-copy step are needed.

Keep generated workflow skill examples aligned with the TrailStep command, `.trailstep` runtime paths, and `trst-*` skill-prefix convention.
