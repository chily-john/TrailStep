---
"@stepkit/core": minor
---

Interactive `claude`-provider steps now pass `--dangerously-skip-permissions` by default; set `permissionMode: "prompt"` on an agent-target entry to restore per-tool confirmation.
