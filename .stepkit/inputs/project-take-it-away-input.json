{
  "conversation": "The project/package/product name StepKit is unavailable and must pivot to TrailStep. Rename all occurrences and public surfaces from stepkit/StepKit/STEPKIT/@stepkit to trailstep/TrailStep/TRAILSTEP/@trailstep. Decisions confirmed by the user: npm package scope changes from @stepkit/* to @trailstep/*; CLI binary changes from stepkit to trailstep; config/runtime paths change from .stepkit/ to .trailstep/; environment variables change from STEPKIT_* to TRAILSTEP_*; internal exported type/function names change too, for example StepKitConfig -> TrailStepConfig and parseStepKitConfig -> parseTrailStepConfig; manifest metadata changes too, for example package.json field stepkit -> trailstep and keyword stepkit-workflow -> trailstep-workflow. Goal: no remaining StepKit/stepkit naming in source/docs/tests/config except unavoidable historical/generated/external references if any are explicitly justified. Exclude generated/build artifacts such as dist, node_modules, .git, graphify-out, coverage, .turbo. Also change the short generated skill prefix currently sk to a TrailStep-specific prefix. Use trst rather than ts because ts is widely recognized as TypeScript and could be ambiguous. Implement repository-wide rename carefully, update tests and docs, run validation, and open a PR."
}
