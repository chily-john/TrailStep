---
"@stepkit/authoring": major
---

Renamed the `@stepkit/sdk` package to `@stepkit/authoring` to better reflect that it's a TypeScript authoring/builder layer over `@stepkit/core`, not a client SDK. Update imports from `@stepkit/sdk` to `@stepkit/authoring` and update the corresponding `package.json` dependency entry.
