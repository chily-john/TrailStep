---
"@trailstep/authoring": major
---

Renamed the `@trailstep/sdk` package to `@trailstep/authoring` to better reflect that it's a TypeScript authoring/builder layer over `@trailstep/core`, not a client SDK. Update imports from `@trailstep/sdk` to `@trailstep/authoring` and update the corresponding `package.json` dependency entry.
